spring:
  datasource:
    url: jdbc:mysql://localhost:3306/batch_test
    username: root
    password: New1234!
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: true

ksign:
  url: http://localhost:8081/ksign